name: itglue_health_check returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: itglue_health_check
  args: {}
  expect:
    is_error: true
timeout: 30s
